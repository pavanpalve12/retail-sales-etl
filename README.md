# retail-sales-etl
End-to-end ETL pipeline that ingests retail CSV data, performs structured data conditioning and dimensional modeling using Pandas, and loads analytics-ready fact and dimension tables into SQLite with full logging and metadata control.
